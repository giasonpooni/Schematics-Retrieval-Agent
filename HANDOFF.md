# Handoff

v0.1.0 ships the IR, retrieval operators, eligibility table, quadratic-drag
fixture, JSON / mermaid / USDA projections, and a plan-only kernel adapter.

Next increments, in order:

1. Optional adapter that wraps a pinned JSPT SHA and replaces fixture A.
2. Optional PLSR evaluate only when that adapter writes `result=SAMPLED` and `fixture=false`.
3. Compile from an authored USDA that already carries Ns* schemas. Refuse incomplete schemas.
4. Bind an RCI digest fixture without promoting millimetres to states.

Do not start a search service.
